# 待办事项

整理日期：2026-09-20。依据：[验收报告.md](验收报告.md)。

当前状态：S1 已完成；S2 已开始、已暂停、未收尾验收；S3—S7 尚未开始。本文仅整理待办，不代表恢复开发或完成验收。

## 一、需要你决定、提供或确认的事项

- [ ] **明确是否恢复 S2 收尾。** 恢复后先复读验收报告与 S2 设计，完成最终静态复核和必要修正，再同步阶段计划及验收报告；本次恢复范围仅限 S2，不直接推进 S3。本地不编译、不运行测试，编译与测试留在 GitHub Actions。
- [ ] **P01：确定发布地址。** 明确远程仓库及镜像发布地址，供后续 Actions 与发布配置使用。
- [ ] **P02：提供目标设备信息。** 补充实际部署设备及系统信息，用于后续 ARM64 Armbian 启动和交互验收。
- [ ] **P03：确认最终目标完成条件。** 明确业务上何时算“目标完成”；旧程序的平仓 success / finished close 记录不能直接视为最终目标完成。
- [ ] **P05：确认账户身份方案。** 当前账户身份仍为占位项，需要明确用于账户识别和关联的方案。
- [ ] **P06：确认配置与部署目录方案。** 确认参数命名、多账户配置文件组织、数据库与备份目录、容器内路径；当前均为设计草案，已有 `.bak` 实现不代表整套方案已确认。
- [ ] **P07：确认旧数据关联方案。** 明确旧数据如何与新系统关联；尚未确认前继续保留占位。
- [ ] **P04：在 S3 技术核实后确认采用方案。** 由开发核实 Gate 客户标识格式、能力及“请求确认未成功”的证据，再据此确认方案；当前测试标识不能视为 Gate 已支持，也不能通过 TUI 手填结果解除未知操作阻塞。

以上 P01—P07 尚无已确认结论；可随对应阶段逐项处理，不能把占位项视为验收通过证据。验收报告未给出各项完整选项，具体内容需结合阶段计划和设计说明确认。


决定:

- [ ] **P01：确定发布地址。** 明确远程仓库及镜像发布地址，供后续 Actions 与发布配置使用。
答: 我的计划是先在本地完成文件修改,然后部署到远程 GitHub,通过GHCR.然后才会进行输出镜像发布地址.所以先把这个待办消除
- [ ] **P02：提供目标设备信息。** 补充实际部署设备及系统信息，用于后续 ARM64 Armbian 启动和交互验收。
答: 把这个待办进行消除,我只需要有 GHCR.  MARM 64 armbian 系统的容器镜像就可以了
- [ ] **P03：确认最终目标完成条件。** 明确业务上何时算“目标完成”；旧程序的平仓 success / finished close 记录不能直接视为最终目标完成。
答: 旧程序的平仓 success / finished close 记录能直接视为最终目标完成。
## 二、恢复后需要安排开发处理的事项

以下为开发与验证工作，不要求你亲自编写代码；应按阶段推进。

### S2 收尾

- [ ] 对现有基础库及 13 组测试源码完成最终静态审查，处理必要修正并完成阶段收尾记录。当前没有编译、链接或测试通过结果。
- [ ] 将 `阶段计划.md` 中 S2 的过时“未开始”状态同步为实际状态，并更新验收报告。
- [ ] 复核配置保存失败的处理：原子替换不等于配置文件与 SQLite 的跨介质事务；若 rename 成功后目录同步或事务收尾失败，必须重新读取配置确认实际状态。

### S3 及后续阶段

- [ ] 完成 HTTP 适配器与 P04 核实，落实未知结果只查询、不直接重发，以及撤单后的状态确认。
- [ ] 改写旧程序的风险行为：下单成功后才写文件、撤单仅检查 HTTP、直接删除非 pending/ongoing/success 状态。必须按新持久化及状态核实规则接入。
- [ ] 补齐后台、TUI、暂停/停止/恢复调度、5 分钟核实、30 秒倒计时、同宿主机账户互斥及备份清理。
- [ ] **S4：实现安全启动与恢复。** 启动先核对，提供手动存储恢复检查及逐个任务恢复；不得根据数据库 running 状态直接发单。后台需串行协调发送与用户命令，衔接当前进程内的存储故障门禁。
- [ ] 接入成交、持仓、人工操作与日志的实际写入、脱敏、备份及保留清理，包括需求中的 30 天日志规则。适配器保存完整 Intent / 结果前必须脱敏，不能包含密钥。
- [ ] 修复测试网络隔离缺口。旧 `test_positions.py` 导入即访问真实接口，新测试必须模拟 HTTP，避免沿用会连接真实接口的旧测试入口。

## 三、正式首版验收前需要取得的结果

- [ ] 全部功能迁移完成，并逐项核对功能迁移清单。
- [ ] GitHub Actions 编译及测试通过，取得可核查结果；现有 13 组测试源码不能代替测试通过证据。
- [ ] 完成镜像发布并取得发布结果。
- [ ] 完成目标 ARM64 Armbian 设备启动验收。
- [ ] 完成 TUI 键盘交互验收。
- [ ] 汇总上述结果并更新正式验收记录；当前尚未开始正式首版验收。
