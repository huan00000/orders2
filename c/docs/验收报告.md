# 阶段验收报告

更新日期：2026-09-20。

## 本次结论

**S2：领域与持久化基础已开始，现按用户要求暂停，尚未完成阶段收尾验收。** S1 已在上次任务完成；S3—S7 尚未开始。下述“本次暂停交接”记录当前实际进度；后文 S1 内容作为历史保留，不代表当前目录仍只有文档。

执行依据：《计划执行.skill》要求先查看企划书、需求及本报告，每次只完成一个阶段，结束后在本报告同步信息。用户最初要求待确认事项先占位，随后明确要求“先暂停这个阶段的操作，在验收报告.md完成信息对称后结束操作”。收到暂停指令后，仅更新本报告，不再修改代码或继续验证。

## 本次暂停交接（S2，2026-09-20）

### 已落地文件

| 文件 | 当前实现内容 |
| --- | --- |
| [CMakeLists.txt](../CMakeLists.txt) | C++20 核心库、SQLite 依赖、CTest 测试入口；尚未配置或构建 |
| [domain.h](../include/orders2/domain.h)、[order_state.cpp](../src/domain/order_state.cpp) | 任务、目标、操作与订单类型；订单状态转换、来源校验、未知核实与撤单成交竞态 |
| [database.h](../include/orders2/database.h)、[database.cpp](../src/storage/database.cpp) | SQLite RAII、参数绑定、外键、WAL/FULL、事务提交及回滚 |
| [store.h](../include/orders2/store.h)、[store.cpp](../src/storage/store.cpp) | 表结构 v1；活动任务/目标/操作唯一约束；Intent 预写与防重；完整结果后写；订单状态和历史同事务；账户存储故障门禁 |
| [config.h](../include/orders2/config.h)、[config.cpp](../src/config/config.cpp) | 旧 `.env` 格式及环境覆盖；全部任务停止后才能保存；校验、同目录临时替换及最近一份 `.bak` |
| [foundation_test.cpp](../tests/foundation_test.cpp) | 13 组测试源码，采用临时数据库/配置和模拟发送回调，不调用真实交易接口 |
| [S2领域与持久化设计.md](S2领域与持久化设计.md) | 依赖选择、表结构、事务时序、故障边界、P06 配置草案与后续衔接 |

关键实现：发送前先提交最小防重复记录及 sending 阶段，再调用发送回调；同 ID 同内容返回已有记录，同 ID 不同内容拒绝；prepared/sending/unknown 均不直接重发。撤单仅收到 HTTP 成功响应仍保留未知操作与取消中订单，不能释放未完成操作名额。结果写入失败阻止该账户继续写入/发送；明确拒绝暂停父目标。响应后的记录时间另取时钟，可注入测试时钟。

### 已进行的检查及未验证内容

- 已复读企划书、需求、阶段计划、迁移清单和原报告；读取旧 `general.py` 及 `.env.example` 对照配置行为。未读取真实密钥、订单或日志文件。
- 已查阅 CMake FindSQLite3、SQLite 事务和部分索引的官方文档，来源链接保存在 S2 设计说明。
- 已人工审阅预写/发送/结果事务、重复 ID、未知结果、撤单确认、存储故障及配置保存路径，并据静态审阅调整代码。
- 已执行 PowerShell 文本检查：9 个 C++ 源码/头文件的本地头文件引用存在，CMake 引用的源码存在，源码无行尾多余空白；数得 13 组测试注册。该检查不等于 C++ 语法、链接或运行测试通过。
- `git diff --check -- .` 未报错，但 c 目录是外层仓库的未跟踪内容，不能据此认定它检查了全部新增文件；新增源码另做上述文本检查。未执行 git add、commit 或 push。
- **没有运行 CMake、编译器、CTest 或旧 Python 测试。13 组测试只有源码，没有通过结果。** 未启动服务、未发出交易请求、未创建远程仓库或发布镜像。

### 剩余工作与已知边界

1. S2 尚待恢复后的最终静态复核、必要修正及阶段收尾。不得把当前暂停记录当作 S2 已验收；编译和测试仍按要求留在 GitHub Actions。
2. 当前仅为基础库，没有后台、TUI 或 HTTP 适配器。暂停/停止/恢复调度、5 分钟核实、30 秒倒计时、同宿主机账户互斥与备份清理属于后续阶段。
3. 存储故障门禁目前在 Store 进程内生效。S4 必须实现启动先核对、手动存储恢复检查及逐个任务恢复，不能根据数据库中的 running 状态直接发单。发送期间释放锁，后续后台必须串行协调发送与用户命令。
4. 成交、持仓、人工操作与日志目前只有表基础；实际写入、脱敏、备份与保留清理待后续接入。完整 Intent/结果需要适配器脱敏，不能含密钥。
5. P03 最终目标完成条件继续占位，无策略自动完成目标的业务入口。P04 客户标识格式及确认未成功的证据待 S3 核实，当前测试标识不代表 Gate 支持；内部证据接口不能作为 TUI 手填结果解除阻塞入口。
6. P06 的 `.bak` 保存方式已实现；参数命名、多账户文件组织、数据库/备份及容器路径仍是设计草案。P01 发布地址、P02 设备信息、P05 账户身份与 P07 旧数据关联仍占位。
7. 配置原子替换不等于文件与 SQLite 的跨介质事务。若 rename 已成功而随后目录同步或事务收尾失败，文件可能已更新，必须重新读取确认；该边界已写入设计说明。
8. 暂停前尝试同步阶段计划/替换报告的补丁因同一路径存在多个操作被工具拒绝，没有落地。因此 `阶段计划.md` 的 S2 状态仍写“未开始”；这是尚未同步的旧状态，实际以本报告“已开始、已暂停、未收尾”为准。恢复时再同步阶段计划。本次依用户要求仅更新验收报告。

### 恢复起点

等待用户明确恢复后，继续 **S2 收尾**：先复读本报告和 S2 设计，对现有实现及测试源码进行最终静态审查，处理必要修正，再同步阶段计划与验收报告。继续遵守本地不编译、不运行测试；不得直接跳到 S3，也不在恢复任务中连做 S3。此次暂停不安排自动恢复或定时任务。

## S1 历史记录（上次任务）

上次开始时本报告为空，c 目录只有文档及空白 1.md，没有 C++ 源码或构建文件。上次完成的是文档与静态审查阶段验收，不是 C++ 产品交付验收。

## 本阶段交付

| 文件 | 内容 |
| --- | --- |
| [阶段计划.md](阶段计划.md) | S1—S7 的范围、依赖、完成判据及 P01—P07 占位事项 |
| [功能迁移清单.md](功能迁移清单.md) | 全部 16 个 Python 文件的业务/诊断/测试去向、调用链、配置状态映射、策略公式、目标条件 C01—C07、测试迁移要求 |
| 本报告 | 阶段结果、验证边界及下次起点 |

## 静态检查结果

- 已读取《企划书.md》《需求.md》《计划执行.skill》及原验收报告；沿目录祖先检查，未发现适用 AGENTS.md。
- 已枚举 orders2 全部 16 个 Python 文件：9 个运行/支持模块、2 个手工诊断脚本、5 个单元测试文件，均纳入迁移清单。
- 已审查主链路源码、配置空模板、旧 Docker/Compose/Actions 文件及测试定义；策略公式与 test_pto/test_main 的样例交叉核对。
- 已区分需求覆盖旧行为的差异：30 天日志、SQLite 历史、防重复预写、未知结果只查询、首次空服务启动、暂停停止及新平台范围。
- 本阶段只新增两份规划文档并填写本报告，未修改原 Python 程序、真实配置或交易数据。
- 未执行本地编译或测试；未启动服务、未发出交易请求、未创建远程仓库、未发布镜像。当前无云端构建或设备验收结果。

## 后续必须处理的发现

1. 原脚本没有目标状态机，平仓 success 仅产生 finished close 记录；最终目标完成条件使用 P03 占位，不能宣称已完成用户确认。
2. 旧追踪订单 text 固定为 apiv4，不足以标识唯一操作；客户标识能力和未知请求失败证据在 S3 核实，当前不宣称 Gate 接口兼容性已验证。
3. 旧下单成功后才写文件、撤单仅检查 HTTP、非 pending/ongoing/success 状态直接删除，必须按新持久化和状态核实规则改写。
4. test_positions.py 导入即访问真实接口；旧 unittest discover 入口有网络隔离缺口，新测试必须模拟 HTTP。
5. 仓库/镜像地址、设备信息、配置路径和迁移关联等按阶段计划 P01—P07 占位；不以占位作为验收通过证据。

## S1 当时记录的下次执行起点（现已开始，见上方暂停交接）

只执行 **S2：领域与持久化基础**。先复读本报告和阶段计划，再建立 CMake、领域类型、订单状态机、SQLite 事务及约束、Intent 预写与配置保存机制，并编写对应云端测试源码；本地保持不编译。完成 S2 后更新本报告，不在同一次任务推进 S3。

正式首版验收仍未开始：全部功能迁移、Actions 编译测试与镜像发布、ARM64 Armbian 启动和 TUI 键盘交互均待后续完成。
