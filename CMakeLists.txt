cmake_minimum_required(VERSION 3.20)
project(orders2 LANGUAGES CXX)
find_package(CURL REQUIRED)
find_package(OpenSSL REQUIRED)
add_executable(orders2 main.cpp)
target_compile_features(orders2 PRIVATE cxx_std_17)
target_link_libraries(orders2 PRIVATE CURL::libcurl OpenSSL::Crypto)
