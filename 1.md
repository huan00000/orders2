我准备把e:\git\0AAimportant\gate_bot\ago\v3\auto_orders\tests\orders2这个仓库提交到github action build成一个GHCR.帮我写一个.github/workflows.build.yml    


预期:-1.main脚本E:\git\0AAimportant\gate_bot\ago\v3\auto_orders\tests\orders2\main.py的日志记录时间的保存区间
修改为最近4周,超过该区间的数据将会被清除.
2.E:\git\0AAimportant\gate_bot\ago\v3\auto_orders\tests\orders2\.github\workflows\build.yml来build一个GHCR到GitHub
.需要注意requirements.txt .env文件类似文件的配置
3.我希望这个run.yml文件可以拉取最新GHCR镜像来运行.在armbian服务器上通过 Docker Compose 运行
如有没说清楚的地方.请向我提问

已完成修改：

  - 日志保留最近 28 天，启动时清理，运行期间每 60 秒检查。
  - build.yml 自动测试并构建 ARM64、ARMv7、AMD64 镜像，发布到 GHCR。
  - run.yml 拉取 latest，订单和日志持久化到 data。
  - 补齐 Dockerfile、requirements.txt、.env.example 和忽略配置，密钥不会进入镜像。

  20 项测试通过，Python 和 YAML 检查通过。本机没有 Docker，尚未实际构建或发布镜像。

  部署步骤见 /E:/git/0AAimportant/gate_bot/ago/v3/auto_orders/tests/orders2/README.md。填写服务器 .env、迁移订单状态后，启动或更新：

  docker compose -f run.yml up -d --pull always

  每次执行该命令会拉取最新镜像；运行中的容器不会自动检测新版本。